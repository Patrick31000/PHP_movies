{% extends 'base.html.twig' %}

{% block title %}Film Listing{% endblock %}

{% block style %}

{% if error %}
<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
{% block body %}
<form id="formlogin" action="{{ path('login') }}" method="post">
    <label for="username">Username:</label>
    <input type="text" id="username" name="_username" value="{{ last_username }}" />

    <label for="password">Password:</label>
    <input type="password" id="password" name="_password" />

    {#
    If you want to control the URL the user
    is redirected to on success (more details below)
    #}

    <input type='hidden' name='_target_path' value='/admin' />
    
    <button type="submit">login</button>
</form>
{% endblock %}

{% endblock %}


<!-- 
<script>
$("#formlogin").submit
if (username===ryan){
    $("#formlogin").append("<input type='hidden' name='_target_path' value='/movies' />");
}
else if (username===admin){
     $("#formlogin").append("<input type='hidden' name='_target_path' value='/admin' />");
}
    </script> -->